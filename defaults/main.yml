avahi_user: "{{ __avahi_user }}"
avahi_group: "{{ __avahi_group }}"
avahi_package: "{{ __avahi_package }}"
avahi_conf_dir: "{{ __avahi_conf_dir }}"
avahi_conf_file: "{{ avahi_conf_dir }}/avahi-daemon.conf"
avahi_config: []

avahi_daemon_service: "{{ __avahi_daemon_service }}"
avahi_daemon_flags: "{{ __avahi_daemon_flags }}"
